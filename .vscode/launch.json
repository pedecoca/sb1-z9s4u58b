{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Hack",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/run_hack.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}